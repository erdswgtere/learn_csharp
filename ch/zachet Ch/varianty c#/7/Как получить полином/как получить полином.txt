ЕСЛИ УЖЕ ДАНА ТАБЛИЦА А НЕ ФУНКЦИЯ ТО ПРОПУСКАЕМ ШАГИ С 1 по 3 и сразу начинаем с 4
1. т.к функция задана аналитически (дана функция а не таблица ) короче дан Лагранж второй степени из вариантов с функцией а не таблицей, то надо сперва сделать эту таблицу(лучше в тетради перед глазами), для этого нужен нормальный калькулятор где хотя бы есть ln(x) и возведение в степень(там варианты все с ln(x)).
2. рисуем таблицу в 2 строки и 4 столбца(1-ый столбец для обозначений y и x (как в ЛР3 короче)) в x записываем иксы данные из условия
3. считаем y для этого пишем функцию в калькулятор а вместо x пишем наши значения и записываем в таблицу (под тем значением x что подставили значение y-полученное )

4. таблица готова теперь идём на сайт https://www.wolframalpha.com/
5. вбиваем x-ы и у-и в строку что перед нами(см скрин1 тут показано как вбить) в скобках пишется [x, y] - то есть на первом месте идут иксы а на втором игрики
6. если всё верно у вас будет полином (скрин2)
теперь вбиваем его куда надо
